namespace App;
public class RelayServer {
    public void Start() { }
    private static int ComputeHash(string s) { return 0; }
}
public interface IPeer { void Send(); }
public struct Point { public int X; }
public enum Mode { Fast, Slow }
